extends PointLight2D


func _ready():
	pass

func _process(_delta):
	pass
	
	#make light pulse
	
	#var start_scale = 1.6
	#var end_scale = 150.0
	#var tween = create_tween()
	#tween.tween_property(self, "texture_scale", start_scale, 0.5).set_trans(Tween.TRANS_CUBIC).set_ease(Tween.EASE_IN_OUT)
	#tween.tween_property(self, "texture_scale", end_scale, 0.5).set_trans(Tween.TRANS_CUBIC).set_ease(Tween.EASE_IN_OUT) 
	#tween.set_loops()
	
